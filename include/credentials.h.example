// Copy this file to  include/credentials.h  and fill in your details.
// credentials.h is git-ignored so your secrets never get committed.
#pragma once

#define WIFI_SSID  "your-network-name"
#define WIFI_PASS  "your-network-password"

// CollegeFootballData.com API key — free, instant: https://collegefootballdata.com/key
// Used as the college (NCAAF) scores / schedule / rankings source so the ESPN
// endpoint isn't hammered. Leave the placeholder to fall back to ESPN for college.
#define CFBD_KEY   "your-cfbd-key"

// balldontlie.io API key — free: https://balldontlie.io  Used as the NFL scores
// source (whole week per request, 5 req/min free). Leave blank to use ESPN.
#define BDL_KEY    ""
